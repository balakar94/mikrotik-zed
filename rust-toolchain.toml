# Pinned Rust toolchain — MSRV 1.94 (rust-analyzer >=1.94).
# Ensures local `cargo` and CI use the same compiler; workflows also pin 1.94 explicitly.
[toolchain]
channel = "1.94"
targets = ["wasm32-wasip2"]
components = ["rustfmt", "clippy", "rust-analyzer"]
profile = "minimal"

